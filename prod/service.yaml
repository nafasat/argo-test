apiVersion: v1
kind: Service
metadata:
  name: myfirstservice
  labels:
    name: nginx-srv
spec:
  ports:
  - port: 9090
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    type: front-node
    app: nginx-app
  type: NodePort
