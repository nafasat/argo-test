apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: nginx-svc
spec:
  type: NodePort 
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  - port: 443
    targetPort: 443
    protocol: TCP
    name: https
  selector:
    type: front-node
    app: nginx-app

